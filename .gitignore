__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
htmlcov/
coverage.xml
*.db
*.db-wal
*.db-shm

# PyCharm project files. Machine specific: the module file names an
# interpreter that means nothing on another machine, and workspace.xml is
# editor layout that changes every session.
.idea/

# Agent worktrees. A worktree is a checkout of this repository living
# inside it, which git records as a gitlink pointing at a commit no clone
# can resolve. One was committed here by accident and has been removed.
.claude/worktrees/
